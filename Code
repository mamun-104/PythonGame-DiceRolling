# Dice Rolling Simulator Python Project
# by, Md Ibrahim Mamun

import random

min=1
max=6

roll_again="yes"

while roll_again == "yes":
  print("Rolling! The Valuses are")
  print(random.randint(min, max), " ", random.randint(min, max))
  

  roll_again = input("Roll again? type yes/no ")
